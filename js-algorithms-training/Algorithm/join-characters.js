/**
 * Kata: Join Characters with Separator
 * Level: Basic Logic
 * Platform: Personal Training
 *
 * Description:
 * Takes an array of characters and joins them into a single string
 * using a hyphen ("-") as a separator.
 */

function joinCharacters() {
  const lettersArray = ["B", "A", "C", "K", "E", "N", "D"];
  console.log(lettersArray.join("-"));
}

joinCharacters();
